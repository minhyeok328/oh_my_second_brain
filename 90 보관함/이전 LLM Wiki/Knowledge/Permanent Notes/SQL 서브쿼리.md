---
type: "permanent"
status: "wiki-standardized"
created: "2026-05-30"
updated: "2026-05-30"
reviewed: "2026-05-30"
tags:
  - 'llm_wiki'
  - 'sql'
  - 'database'
source:
  - 'C:\lecture'
---

# SQL 서브쿼리

태그: #sql #database #llm_wiki

## 한 줄 정의

쿼리 안에 포함된 또 다른 쿼리로 중간 결과를 조건이나 테이블처럼 사용하는 방식이다.

## 왜 중요한가

단계적 사고를 SQL 안에 표현할 수 있다. 단, 복잡해지면 CTE나 뷰로 분리하는 것이 좋다.

## 핵심 개념

- `WHERE` 조건, `FROM` 절, `SELECT` 절에서 사용할 수 있다.
- 상관 서브쿼리는 바깥 행마다 실행될 수 있어 비용이 커진다.
- 가독성과 성능을 함께 확인해야 한다.

## 예제

```sql
SELECT * FROM products
WHERE price > (SELECT AVG(price) FROM products);
```

## 실무 활용

평균 이상 상품 조회, 최신 이벤트 필터링, 단계적 리포트 작성에 사용한다.

## 관련 개념

- [[SQL SELECT와 WHERE]]
- [[SQL GROUP BY]]
- [[SQL JOIN]]

자료 힌트: 02_mysql_workspace

## 내 말로 다시 설명

SQL 서브쿼리은/는 강의에서 나온 개념을 정의, 사용 조건, 주의점, 연결 개념으로 다시 설명하기 위한 LLM wiki 원자 노트다. 단순 암기보다 실제 문제에서 언제 꺼내 쓸지 판단하는 데 초점을 둔다.

## 언제 쓰는가

- 강의 실습 코드에서 같은 개념을 다시 만날 때
- 프로젝트에서 관련 오류나 설계 결정을 설명해야 할 때
- [[SQL SELECT와 WHERE]], [[SQL GROUP BY]], [[SQL JOIN]]와 함께 문제 원인을 좁힐 때

## 언제 쓰면 안 되는가

- 구체적인 API 버전, 파라미터, 보안 정책이 필요한 경우에는 공식 문서를 먼저 확인한다.
- 예제 하나만 보고 모든 상황에 일반화해야 할 때는 보류한다.
- 이미 더 좁고 구체적인 노트가 있는 경우에는 그 노트로 이동한다.

## 자주 헷갈리는 점

- 이름이 비슷한 인접 개념과 책임 범위가 다를 수 있다.
- 강의 예제의 상황과 실제 프로젝트의 데이터, 환경, 버전 차이를 분리해야 한다.
- "동작한다"와 "운영에서 유지보수 가능하다"는 다른 기준이다.

## 확인 질문

- SQL 서브쿼리을/를 쓰면 어떤 문제를 더 단순하게 설명할 수 있는가?
- 관련 개념과 구분되는 핵심 기준은 무엇인가?
- 실제 프로젝트에 적용하면 먼저 검증해야 할 실패 조건은 무엇인가?
